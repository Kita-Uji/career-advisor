---
phase: 01-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MessageBubble.jsx
  - src/components/ChatInput.jsx
autonomous: false
requirements:
  - BUBBLE-01
  - BUBBLE-02
  - BUBBLE-03
  - INPUT-01
  - INPUT-02

must_haves:
  truths:
    - "Assistant message bubbles show no avatar circle and no CA initials"
    - "Assistant bubbles have a soft indigo-50 background (not white)"
    - "User bubbles are visually unchanged (indigo-600, same shape, same sizing)"
    - "The chat input renders as an elevated floating card (shadow-lg, rounded-2xl) lifted off the background"
    - "The input feels like the focal point of the page — not a flat border strip at the bottom"
  artifacts:
    - path: "src/components/MessageBubble.jsx"
      provides: "Updated assistant bubble rendering without avatar"
      contains: "bg-indigo-50"
    - path: "src/components/ChatInput.jsx"
      provides: "Elevated floating card input wrapper"
      contains: "shadow-lg rounded-2xl"
  key_links:
    - from: "src/components/MessageBubble.jsx"
      to: "assistant branch"
      via: "isUser === false renders div WITHOUT avatar div"
      pattern: "bg-indigo-50"
    - from: "src/components/ChatInput.jsx"
      to: "outer wrapper div"
      via: "shadow-lg and rounded-2xl classes on the container"
      pattern: "shadow-lg.*rounded-2xl|rounded-2xl.*shadow-lg"
---

<objective>
Apply targeted visual polish to MessageBubble and ChatInput so the chat interface feels calm, minimal, and trustworthy. The assistant bubble loses its avatar clutter and gains a soft indigo-50 background. The input transforms from a flat border strip into an elevated floating card that draws the eye.

Purpose: The core value of this product is a focused, trustworthy conversation experience. Current visual noise (avatar, flat input strip) works against that.
Output: Two updated component files. No new files, no layout changes, no feature additions.
</objective>

<execution_context>
@C:/Users/Aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-polish/01-CONTEXT.md

<interfaces>
<!-- Current source code. Executor edits these directly — no codebase exploration needed. -->

Current src/components/MessageBubble.jsx (25 lines):
```jsx
export default function MessageBubble({ message }) {
  const isUser = message.role === 'user';

  if (isUser) {
    return (
      <div className="flex justify-end mb-3">
        <div className="bg-indigo-600 text-white rounded-2xl rounded-tr-sm px-4 py-2.5 max-w-[75%] whitespace-pre-wrap text-sm leading-relaxed">
          {message.content}
        </div>
      </div>
    );
  }

  return (
    <div className="flex items-start gap-2.5 mb-3">
      <div className="flex-shrink-0 w-8 h-8 rounded-full bg-indigo-100 text-indigo-700 flex items-center justify-center text-xs font-bold">
        CA
      </div>
      <div className="bg-white rounded-2xl rounded-tl-sm px-4 py-2.5 max-w-[75%] shadow-sm whitespace-pre-wrap text-sm leading-relaxed text-gray-800">
        {message.content}
      </div>
    </div>
  );
}
```

Current src/components/ChatInput.jsx (55 lines):
```jsx
import { useState, useRef, useEffect } from 'react';

export default function ChatInput({ onSend, isStreaming }) {
  const [text, setText] = useState('');
  const textareaRef = useRef(null);

  useEffect(() => {
    const el = textareaRef.current;
    if (!el) return;
    el.style.height = 'auto';
    el.style.height = Math.min(el.scrollHeight, 120) + 'px';
  }, [text]);

  function handleKeyDown(e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      submit();
    }
  }

  function submit() {
    const trimmed = text.trim();
    if (!trimmed || isStreaming) return;
    onSend(trimmed);
    setText('');
  }

  return (
    <div className="border-t border-gray-200 bg-white px-4 py-3">
      <div className="flex items-end gap-2">
        <textarea
          ref={textareaRef}
          rows={1}
          value={text}
          onChange={(e) => setText(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="Ask me anything about your career..."
          disabled={isStreaming}
          className="flex-1 resize-none rounded-xl border border-gray-300 px-3 py-2 text-sm leading-relaxed focus:outline-none focus:ring-2 focus:ring-indigo-400 disabled:opacity-50 overflow-hidden"
          style={{ minHeight: '40px', maxHeight: '120px' }}
        />
        <button
          onClick={submit}
          disabled={isStreaming || !text.trim()}
          className="flex-shrink-0 w-10 h-10 rounded-xl bg-indigo-600 text-white flex items-center justify-center hover:bg-indigo-700 disabled:opacity-40 disabled:cursor-not-allowed transition-colors"
          aria-label="Send"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" className="w-5 h-5">
            <path d="M3.478 2.405a.75.75 0 00-.926.94l2.432 7.905H13.5a.75.75 0 010 1.5H4.984l-2.432 7.905a.75.75 0 00.926.94 60.519 60.519 0 0018.445-8.986.75.75 0 000-1.218A60.517 60.517 0 003.478 2.405z" />
          </svg>
        </button>
      </div>
    </div>
  );
}
```

App.jsx places ChatInput as fixed footer inside a `max-w-3xl mx-auto w-full` flex column. The outer wrapper of ChatInput is what receives the card treatment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish MessageBubble — remove avatar, soften assistant background</name>
  <files>src/components/MessageBubble.jsx</files>
  <action>
Rewrite the assistant branch of MessageBubble (the `isUser === false` return). Make two targeted changes:

1. Remove the avatar entirely — delete the outer `flex items-start gap-2.5` wrapper and the `div` containing the CA initials circle (`w-8 h-8 rounded-full bg-indigo-100 text-indigo-700 ...`). The assistant bubble div renders as a direct child with no wrapper needed beyond the `mb-3` container.

2. Change the bubble background from `bg-white` to `bg-indigo-50`.

Keep everything else exactly as-is: `rounded-2xl rounded-tl-sm`, `px-4 py-2.5`, `max-w-[75%]`, `shadow-sm`, `whitespace-pre-wrap`, `text-sm leading-relaxed`, `text-gray-800`.

The user branch (`isUser === true`) must not be touched — indigo-600 bubble stays identical.

Use your discretion on whether to expand `max-w-[75%]` slightly (e.g., to `max-w-[80%]`) now that the avatar no longer consumes horizontal space. Keep it subtle — the goal is calm, not dramatic.

The final assistant branch should render a single container div with `mb-3` and inside it the bubble div directly (no gap wrapper, no avatar sibling).
  </action>
  <verify>
    <automated>cd /c/Users/Aaron/Desktop/claude/career-advisor && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - No avatar div or CA text in assistant bubble
    - Assistant bubble background is bg-indigo-50
    - User bubble unchanged (bg-indigo-600)
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish ChatInput — replace border strip with elevated floating card</name>
  <files>src/components/ChatInput.jsx</files>
  <action>
Replace the outer wrapper `div` of the return statement. Currently: `border-t border-gray-200 bg-white px-4 py-3`. Change to an elevated floating card feel.

New outer wrapper: `bg-gray-50 px-4 py-3` (remove the top border, let the card itself define the visual boundary).

Inside, wrap the existing `flex items-end gap-2` row in a new card div: `bg-white shadow-lg rounded-2xl px-4 py-3`. The flex row content (textarea + button) moves inside this card div. Adjust padding so the card has comfortable internal spacing — the outer wrapper provides vertical rhythm (e.g., `py-3`) and the card provides the visual elevation.

Use your discretion on:
- Whether the textarea keeps its `border border-gray-300` or goes borderless (the card acts as container). Borderless (`border-0 focus:ring-0`) inside a card often looks cleaner — try it.
- Exact padding values to achieve a "lifted" look (the card should have breathing room inside).

Do NOT change any of the textarea's functional attributes (ref, rows, value, onChange, onKeyDown, placeholder, disabled, style). Do NOT change the button — only the outer structure changes.

The functional behavior (auto-resize, Enter to submit, Shift+Enter for newline, disabled during streaming) must remain identical.
  </action>
  <verify>
    <automated>cd /c/Users/Aaron/Desktop/claude/career-advisor && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Outer wrapper has no border-t, uses gray-50 background (or transparent)
    - Inner card has shadow-lg and rounded-2xl
    - Input card visually lifts off the page background
    - Textarea and send button still work (resize, submit, disable)
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — calm, minimal, focused</name>
  <files>src/components/MessageBubble.jsx, src/components/ChatInput.jsx</files>
  <action>Start the dev server and verify the visual result looks calm, minimal, and trustworthy. Both component changes must be visible and correct before marking done.</action>
  <what-built>MessageBubble polish (no avatar, indigo-50 background) and ChatInput elevated card (shadow-lg, rounded-2xl)</what-built>
  <how-to-verify>
1. Run: `npm run dev` in the career-advisor directory
2. Open http://localhost:5173 in the browser
3. Check the input area at the bottom — it should appear as a floating white card with a visible shadow, lifted off the gray background. No flat border strip.
4. Send a message or look at the initial greeting — the assistant bubble should have a soft indigo-50 background (barely-there lavender tint, not white). No CA circle avatar.
5. Reply as the user (or look at any user message) — indigo-600 bubble, unchanged.
6. Confirm the overall feel: calm, focused, minimal. The input draws the eye. No visual clutter.
  </how-to-verify>
  <verify>
    <automated>cd /c/Users/Aaron/Desktop/claude/career-advisor && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Human approves the visual result as calm, focused, and trustworthy</done>
  <resume-signal>Type "approved" if it looks right, or describe what looks off</resume-signal>
</task>

</tasks>

<verification>
Build passes (no TypeScript/ESLint errors).
No avatar div exists in MessageBubble assistant branch.
Assistant bubble contains bg-indigo-50 class.
User bubble contains bg-indigo-600 class (unchanged).
ChatInput outer wrapper has no border-t border-gray-200.
ChatInput inner card has shadow-lg and rounded-2xl.
Human verifies the visual result looks calm and minimal.
</verification>

<success_criteria>
- BUBBLE-01: Assistant bubbles render with no avatar circle or CA initials
- BUBBLE-02: Assistant bubbles display bg-indigo-50 background
- BUBBLE-03: User bubbles visually unchanged (bg-indigo-600, same shape/sizing)
- INPUT-01: Chat input displays as elevated card with shadow-lg and rounded-2xl
- INPUT-02: Input card lifts off the bottom bar — no flat border strip, floating feel
- Human approves the visual result as calm, focused, and trustworthy
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-polish/01-01-SUMMARY.md` with:
- What was changed in each file
- Key class changes (removed/added)
- Any discretion choices made (avatar wrapper structure, textarea border decision, max-width adjustment)
- Confirmation that all 5 requirements are met
</output>
